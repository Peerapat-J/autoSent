# autoSent

macOS utility for scheduling one Return key press in an existing LINE message draft.

Development happens on the `dev` branch. Review and merge changes into `main` through a pull request.
