# autoSent

แอป macOS สำหรับตั้งเวลา **กด Return/Enter หนึ่งครั้ง** ในข้อความร่างที่พิมพ์ไว้แล้วใน LINE แอปไม่ได้บันทึกข้อความลงไฟล์ ข้อความร่างที่ตรวจจับจะอยู่ในหน่วยความจำขณะรอส่งเท่านั้น

## ใช้งาน

1. เปิด LINE เข้าไปในห้องที่ต้องการส่งและพิมพ์ข้อความร่างให้ครบ จะใช้ **หน้าต่างหลัก** หรือเปิดเป็น **หน้าต่างแชตแยก** ก็ได้ ตรวจด้วยว่าตั้งค่า Send key ใน LINE เป็น **Enter**
2. เปิด `autoSent.app` เลือกรูปแบบหน้าต่างให้ตรงกับ LINE เลือกวันและเวลาส่ง กำหนดว่าจะยอมให้ส่งช้าได้สูงสุดกี่นาที (1–240 นาที; ค่าเริ่มต้น 15 นาที) แล้วกด **เตรียมส่ง**
3. ครั้งแรก macOS จะขอสิทธิ์ Accessibility ให้เปิดที่ **System Settings → Privacy & Security → Accessibility** แล้วกลับมากด **เตรียมส่ง** อีกครั้ง
4. ภายใน 5 วินาที กลับไปที่ LINE แล้วคลิกช่องพิมพ์ข้อความร่าง แอปจะจับหน้าต่าง ห้องที่เลือก ช่องพิมพ์ และข้อความเดิมไว้ ถ้าหน้าต่างหลักไม่เปิดเผยรายการห้องที่เลือกผ่าน Accessibility หรือหน้าต่างแชตแยกไม่เปิดเผยชื่อห้อง แอปจะไม่ตั้งเวลา
5. เปิด autoSent และ LINE ค้างไว้จนถึงเวลา **อย่าเปลี่ยนห้อง LINE หรือแก้ไขข้อความร่าง** เมื่อถึงเวลา แอปจะเปิดหน้าต่างเดิม ตรวจห้องที่เลือก ช่องพิมพ์ และข้อความอีกครั้ง แล้วส่ง Return หนึ่งครั้ง

ถ้า LINE ปิด ถูกเปิดใหม่ หน้าต่างหรือชื่อห้องเปลี่ยน ช่องพิมพ์เปลี่ยน ข้อความร่างเปลี่ยน หรือสิทธิ์ Accessibility ถูกปิด แอปจะไม่กด Enter เช่นกันถ้าเวลาที่กำหนดผ่านไปเกินช่วงส่งช้าที่เลือกตอนเครื่องตื่นหรือแอปกลับมาทำงาน แอปบันทึกผลล่าสุดพร้อมสาเหตุไว้ในเครื่องและแจ้งเตือนเมื่อได้รับอนุญาตจาก macOS ผลจะแยก **ยังไม่ได้กด Enter**, **ยกเลิกก่อนกด Enter**, และ **โพสต์ปุ่ม Enter แล้ว** การปิดฝาเครื่อง การสั่ง Sleep เอง การออกจากระบบ หรือการรีสตาร์ตยังขัดจังหวะงานได้ ถ้าแอปหยุดระหว่างรอหรือขณะกด Enter แอปจะแจ้งเมื่อเปิดอีกครั้ง โดยกรณีขณะกดจะระบุว่าสถานะไม่แน่ชัดให้ตรวจใน LINE เอง แอปไม่สามารถยืนยันว่า LINE ส่งข้อความถึงผู้รับแล้ว

สำหรับหน้าต่างหลัก การผูกห้องใช้ตัวตนของหน้าต่าง รายการห้องที่เลือก ชื่อห้อง และรหัส Accessibility ของรายการหาก LINE เปิดเผย ส่วนหน้าต่างแชตแยกใช้ตัวตนของหน้าต่างกับชื่อห้อง ถ้า LINE ไม่เปิดเผยข้อมูลที่ต้องใช้ตรวจห้อง แอปจะไม่กด Enter LINE อธิบายวิธีเปิดหน้าต่างแยกและการตั้งค่า Send key ใน [คู่มือ LINE Desktop](https://help.line.me/line/desktop/?contentId=20005810)

## สร้างและตรวจสอบ

ต้องใช้ macOS 13 ขึ้นไป และ Mac Apple silicon เปิด `autoSent.xcodeproj` ใน Xcode แล้วเลือก scheme **autoSent** เพื่อใช้ **Product → Build** หรือ **Product → Test** แอปที่ Xcode สร้างอยู่ใน DerivedData

ถ้าต้องการสร้างแอปที่ `build/autoSent.app` หรือรันชุดทดสอบผ่าน Terminal ให้ใช้ Xcode Command Line Tools:

```sh
./scripts/build.sh
./scripts/test.sh
```

แอปที่สร้างอยู่ที่ `build/autoSent.app` ตัวทดสอบครอบคลุมเงื่อนไขที่ต้องผ่านก่อนส่งจริง รวมกรณีต่างห้องแต่ข้อความเหมือนกันและกรณีเครื่องตื่นช้า การทดสอบส่งจริงควรใช้ห้อง LINE ทดสอบโดยผู้ใช้เอง หลังเปิดสิทธิ์ Accessibility และตรวจว่าข้อความส่งเพียงครั้งเดียว
