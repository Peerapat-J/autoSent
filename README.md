# autoSent

แอป macOS สำหรับตั้งเวลา **กด Return/Enter หนึ่งครั้ง** ในข้อความร่างที่พิมพ์ไว้แล้วใน LINE แอปไม่ได้คัดลอกหรือบันทึกข้อความลงไฟล์ ข้อความร่างที่ตรวจจับจะอยู่ในหน่วยความจำขณะรอส่งเท่านั้น

## ใช้งาน

1. เปิด LINE เข้าไปในห้องที่ต้องการส่ง แล้วพิมพ์ข้อความร่างให้ครบก่อน
2. เปิด `autoSent.app` เลือกวันและเวลาส่ง แล้วกด **เตรียมส่ง**
3. ครั้งแรก macOS จะขอสิทธิ์ Accessibility ให้เปิดที่ **System Settings → Privacy & Security → Accessibility** แล้วกลับมากด **เตรียมส่ง** อีกครั้ง
4. ภายใน 5 วินาที กลับไปที่ LINE แล้วคลิกช่องพิมพ์ข้อความร่าง แอปจะจับช่องพิมพ์และข้อความเดิมไว้
5. เปิด autoSent และ LINE ค้างไว้จนถึงเวลา **อย่าเปลี่ยนห้อง LINE หรือแก้ไขข้อความร่าง** เมื่อถึงเวลา แอปจะเปิด LINE ตรวจว่าช่องพิมพ์และข้อความยังตรงกับที่จับไว้ แล้วส่ง Return หนึ่งครั้ง

ถ้า LINE ปิด ถูกเปิดใหม่ ช่องพิมพ์เปลี่ยน ข้อความร่างเปลี่ยน หรือสิทธิ์ Accessibility ถูกปิด แอปจะไม่กด Enter และจะแสดงเหตุผลในหน้าต่าง autoSent หลังครบเวลา แอปจะเลิกกันหน้าจอดับจากการไม่ได้ใช้งานทันที การปิดฝาเครื่อง การสั่ง Sleep เอง การออกจากระบบ หรือการรีสตาร์ตยังขัดจังหวะงานได้ และแอปไม่สามารถยืนยันว่า LINE ส่งข้อความถึงผู้รับแล้ว

## สร้างและตรวจสอบ

ต้องใช้ macOS 13 ขึ้นไป, Mac Apple silicon และ Xcode Command Line Tools

```sh
./scripts/build.sh
./scripts/test.sh
```

แอปที่สร้างอยู่ที่ `build/autoSent.app` ตัวทดสอบครอบคลุมเงื่อนไขที่ต้องผ่านก่อนส่งจริง การทดสอบส่งจริงควรใช้ห้อง LINE ทดสอบโดยผู้ใช้เอง หลังเปิดสิทธิ์ Accessibility และตรวจว่าข้อความส่งเพียงครั้งเดียว
